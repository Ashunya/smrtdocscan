<ui:FluentWindow x:Class="SmartDocScan.Desktop.Views.SsoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Microsoft Single Sign-On" Height="650" Width="520"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round"
        WindowStartupLocation="CenterOwner">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Title="Sign in with Microsoft" Grid.Row="0" />

        <Grid Grid.Row="1">
            <WebBrowser x:Name="Browser" Navigated="OnBrowserNavigated" LoadCompleted="OnBrowserLoadCompleted" />
        </Grid>
    </Grid>
</ui:FluentWindow>
