<ui:FluentWindow x:Class="SmartDocScan.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:SmartDocScan.Desktop"
        mc:Ignorable="d"
        Title="SmartDocScan Desktop" Height="700" Width="1200"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Title="SmartDocScan Desktop" Grid.Row="0" />

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Folder Tree -->
            <TreeView Grid.Column="0" 
                      ItemsSource="{Binding Categories}" 
                      SelectedItemChanged="TreeView_SelectedItemChanged"
                      BorderThickness="0" 
                      Margin="10">
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding SubCategories}">
                        <StackPanel Orientation="Horizontal" Margin="2">
                            <ui:SymbolIcon Symbol="Folder24" Margin="0,0,8,0" Foreground="#E8B042"/>
                            <TextBlock Text="{Binding Name}" VerticalAlignment="Center"/>
                        </StackPanel>
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>

            <!-- Splitter -->
            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" Background="Transparent"/>

            <!-- Document Grid -->
            <ListBox Grid.Column="2" 
                     ItemsSource="{Binding Documents}" 
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     BorderThickness="0" 
                     Margin="10"
                     Background="Transparent">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel IsItemsHost="True" />
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Width="160" Margin="10">
                            <Border BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Background="White" Padding="4">
                                <Image Source="{Binding ThumbnailUrl}" Height="200" Stretch="Uniform"/>
                            </Border>
                            <TextBlock Text="{Binding DocumentName}" TextWrapping="Wrap" TextAlignment="Center" Margin="0,8,0,0" FontWeight="Medium"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Grid>
</ui:FluentWindow>
